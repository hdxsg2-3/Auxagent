# 2026-07-18

## 上传 GitHub 后续：远程仓库已初始化（含 README）的合并处理
用户已创建 GitHub 仓库 `https://github.com/hdxsg2-3/agent` 并勾选了 README，导致远程已有初始提交。本地仓库独立初始化，二者无共同历史，直接 push 会被拒绝。处理方案：在 `doncx` 目录执行 `git remote add origin`、`git fetch origin`、`git merge origin/main --allow-unrelated-histories` 拉取 README.md 合并本地历史，然后再 `git push -u origin main`。注意需用 PAT/SSH 认证。若已勾选 `.gitignore` 模板，合并后可能产生冲突，需按 git 提示处理。强烈建议后续轮换 API 密钥。

## 上传 GitHub 问题：fetch 时 Connection was reset
用户执行 `git fetch origin` 时报 `Recv failure: Connection was reset`，`git merge` 因无 origin/main 失败，`git push` 因远程含未合并提交被拒绝。根因是**网络无法稳定连接 GitHub**。建议：1) 检查是否开启 VPN/代理，若已开启则配置 `git config --global http.proxy http://127.0.0.1:代理端口` 和 `https.proxy`；2) 重试 `git fetch origin`；3) 如 HTTPS 仍不通，改用 SSH 或删除远程仓库重建空仓库后直接 push；4) 仍失败则换网络/热点。

## 安全：剥离硬编码密钥 + 初始化 Git 仓库准备上传 GitHub
用户要上传项目到 GitHub。发现重大隐患：`backend/config.py` 原硬编码了**真实可用的密钥**（方舟 API Key `ark-0c55ec50-...-50281`、火山内容安全/翻译 AK/SK），上传即泄露。

处理：
- `backend/config.py` 改为从环境变量读取，新增 `_load_dotenv()`（纯标准库，读项目根 `doncx/.env` 或 `backend/.env`），默认值留空/占位符，**不再写死真实密钥**。
- 项目根 `doncx/.env`（含原真实密钥值，本地运行用）已创建，并被 `.gitignore` 忽略，**不提交**。
- 创建 `.env.example` 作模板（无真实值）。
- 创建 `.gitignore`：忽略 `.env`、`*.db`、`node_modules/`、`__pycache__/`、`dist/`、`frontend/.vite/`、`.workbuddy/` 等。
- 在 `doncx` 目录 `git init -b main` 独立仓库（父目录 `Computer` 已有空 `.git`，需避免误传父目录）。`git add -A` 后移除误暂存的 `frontend/.vite/` 与 `backend/package-lock.json`。本地提交 39 文件，核验 `.env/app.db/.vite` 未入库。

用户后续需自行：GitHub 网页建仓库 → `git remote add origin <url>` → `git push -u origin main`（需 PAT/SSH 认证）。**强烈建议去火山方舟/火山引擎控制台轮换上述泄露过的密钥**（虽未上传，但曾明文落盘）。本地运行仍靠 `.env`，勿删。

## 完成：新增"链接一键生成上架文案"全自动 Agent 链路
在现有 Flask+Vue 项目（文案生成/合规审查/客服应答三模块已存在）中，按需求新增 1688 链接 → 自动上架文案闭环：

后端改动：
- `services/prompt_manager.py` 新增 `get_refine_prompt`（原文→核心卖点/参数/场景/简介）。
- `services/llm_service.py` 新增 `refine_product_info` + `_parse_refine_response`。
- `services/scraper.py`（新建）1688 爬虫：requests + 标准库 html.parser，反爬请求头+随机延时，解析标题/规格/详情/优势，失败抛 ScrapeError。
- `controllers/auto_listing.py`（新建）蓝图 `/api/auto-listing`，编排 抓取→提炼→英西德 Listing(并行)→风控(并行)→统一输出；提炼失败兜底用原文。
- `utils/validator.py` 新增 `AutoListingSchema`；`app.py` 注册蓝图；`frontend/src/api/client.js` 新增 `autoListingAPI`（超时 600s）。

前端改动：
- `frontend/src/components/Copywriter.vue` 增加"手动输入/链接一键生成"切换；链接模式含 1688 输入框(校验)、一键生成、4 板块结果(原始抓取/AI提炼/多语种Listing/风控报告)均可复制、异常 ElMessage 弹窗。

验证：
- 后端 py_compile 全通过；scraper 的 _PageParser 单元测试通过（标题/规格/优势提取正确）。
- 前端 `npm run build` 成功（3095 模块）。
- 沙箱缺 flask-cors/marshmallow（项目既有依赖，.venv 为空），已补 `backend/requirements.txt`。

## 修复：手动文案生成 500 错误
问题：前端调用 `/api/copywriter/generate` 返回 500，原因是后端进程未重启、加载的仍是旧代码，旧 `language_map` 没有 `zh`，导致 `KeyError`。

修改：
- `services/prompt_manager.py`：`get_copywriter_prompt` / `get_translate_prompt` 改用 `language_map.get(target_language, fallback)`，避免未知语种直接 KeyError；保留 `zh` 正常支持。
- `services/llm_service.py`：`generate_copywriter` / `translate_copywriter` 在空响应或异常时返回 `{'error': ...}` 而不是 `None`，让控制器能返回 400 友好错误。
- `controllers/copywriter.py`：`generate` / `translate` 接口整体加 `try/except`，异常时返回 JSON 500 并带具体错误信息。

验证：
- py_compile 全通过；
- 各语种（en/es/de/zh/fr）提示词生成均正常；
- 前端 `npm run build` 成功。

注意：Flask 开发服务器必须重启才能加载新增/修改的代码；前端 `npm run dev` 自动热更新无需重启。

## 复查：用户提供的火山方舟 API 配置
用户贴出真实配置：VOLC_ARK_AK / ARK_ENDPOINT_ID / 火山内容安全AK、SK / 火山机器翻译AK、SK / SERVER_PORT=8080。
核对结论：`backend/config.py` 已写死正确值 —— API_KEY=ark-...、API_ENDPOINT=https://ark.cn-beijing.volces.com/api/v3、MODEL_NAME=ep-...（方舟用 endpoint ID 作 model 字段，正确）、内容安全/机器翻译 AK、SK 均在。
`utils/api_client.py` 用 `f'{API_ENDPOINT}/chat/completions'` + `Authorization: Bearer {API_KEY}` 调方舟，集成正确。**500 与配置无关，是后端未重启加载旧代码（缺 zh）所致。**
待确认（已向用户提问，未擅自改动）：
1. 后端现跑 5000，用户写 SERVER_PORT=8080，是否改端口并同步前端代理；
2. 当前风控/翻译均由大模型完成，用户另配火山内容安全、机器翻译专用 API，是否改用专用 API。

## 已实施：翻译/风控改用火山专用 API（用户选"都改"，端口保持 5000）
- 新增 `utils/volc_sign.py`：火山 V4 签名（HMAC-SHA256 + x-content-sha256）。
- `utils/api_client.py` 增加 `call_translate`（translate.volcengineapi.com，Action=TranslateText&Version=2020-06-01，逐文本翻译）与 `call_content_security`（content.volcengineapi.com，Action=TextSliceRisk）。
- `services/llm_service.py`：
  - `translate_copywriter` 优先调机器翻译逐字段翻译并拼回 {title,bullet_points,description}，失败回退大模型；
  - `scan_compliance` 在配置了 `VOLC_CONTENT_APP_ID` 时优先调内容安全并 `_map_content_security` 映射，否则/失败回退大模型。
- `config.py` 增加 `VOLC_CONTENT_APP_ID`（默认空）。
- 验证：py_compile 全过；signer 冒烟测试通过；前端 build 通过。
- 限制：机器翻译可直接用（AK/SK 已具备）；内容安全需控制台审核资产 AppId（用户未提供），且 businessSecurity 返回结构需真实样本才能精确映射，已保守处理（识别不出则回退大模型）。已请用户补 AppId。

## 需求补充（用户反馈）
1. 文案生成增加「中文」语种：手动模式下拉加入中文；链接一键生成 LANGUAGES 由英/西/德扩为英/西/德/中四语种（auto_listing.py + 前端 listingLangs + copyAllLink 遍历）。prompt_manager 两个 language_map 与 validator 的 OneOf 均加入 zh。
2. 客服回复增加中文翻译供商家核对 + 约束真实简洁：
   - 重写 get_customer_service_prompt（同语言回复、2-4 句、禁编造订单/物流/退款、返回 response_zh；需卖家确认则 status=pending）。
   - _parse_customer_service_response 兜底 response_zh。
   - CustomerService.vue 消息加 response_zh，新增"中文翻译(供商家核对)"板块，接线"复制回复/复制翻译"按钮。
3. 验证：后端 py_compile 全过；前端 npm run build 成功(3095 模块)；客服解析单测 response_zh 处理正确。

## 修复：1688 链接一键生成 500 / 抓取失败
问题：用户粘贴的 1688 链接返回 500。实测原因：
1. 后端旧代码用 `detail.1688.com` + PC 头，1688 返回 anti-bot punish 页（`x5secdata`），旧代码未优雅处理导致 500；
2. 用户未重启后端加载新代码。

修改：
- `services/scraper.py` 重写升级：
  - 优先 Playwright 渲染抓取，未安装时回退 requests；
  - requests 模式改为 `m.1688.com` 移动端 + 清洗 URL 追踪参数 + 完整移动浏览器头 + 随机延时；
  - 新增内嵌 JSON 提取（`__INITIAL_STATE__` 等）及 `img alt` 提取，补强规格/卖点；
  - 反爬检测更严格，返回带解决方案的友好错误消息；
- `controllers/auto_listing.py`：`generate` 整个接口加顶层 try/except，防止未捕获异常 500，并返回具体错误信息；
- `frontend/src/components/Copywriter.vue`：新增 `linkResultError` 与错误卡片展示，长错误消息可读；
- `backend/requirements.txt`：添加可选 `playwright` 注释。

验证：
- 后端 py_compile 全过；
- 前端 `npm run build` 成功；
- 沙箱首次请求 `m.1688.com` 可返回真实 HTML 并解析出标题；后续因 IP 高频请求触发反爬，符合预期。

用户操作：
1. 后端依赖若未装则 `pip install -r requirements.txt`；
2. 安装 Playwright 可大幅提升成功率：`pip install playwright && playwright install chromium`；
3. 必须重启 Flask 后端：`python app.py`。

## 安装：cross-border-review-skill（来自 SkillHub）
按 https://skillhub.cn/install/skillhub.md 安装。流程与结论：
- skillhub CLI 安装包需联网下载；沙箱拦截 D:\tmp 与 MSYS 盘符路径，最终用相对路径把 kit 下载到工作区并解包，直接用托管 Python 运行 `skills_store_cli.py install cross-border-review-skill --dir ...`。
- **路径坑**：Git Bash 下 `$HOME`=/c/Users/hdx，Python `Path.resolve()` 把它解析成 `D:\c\Users\hdx\...`（错误位置）。改用 `C:/Users/hdx/.workbuddy/skills` 显式 Windows 路径后正确落盘。最终位置：`C:\Users\hdx\.workbuddy\skills\cross-border-review-skill\`（user-level，WorkBuddy 可识别）。
- 安全审计（CLI + 技能文件）：CLI 仅用 urllib+zipfile（extractall 带 `filter="data"` 防穿越），无 eval/exec、无外联；技能内含 `cross_border_review_skill.py`(OpenClaw SDK 脚本，API 写死返回 demo 数据，无恶意代码)、`install.sh`(含 `pip install openclaw requests`，未执行)、SKILL.md/usage.txt(纯文案)。
- 评级：**P2 安全**，但功能提示：(1) 该技能是 OpenClaw 专用脚本，在 WorkBuddy 下因 import openclaw 缺失不会真正运行；(2) 自带 install.sh 会自动 pip 装包，不符合 WorkBuddy 技能机制，已明确未执行。保留安装，待用户决定。
- 工作区临时文件（kit.tar.gz/.skillhub_kit/.skillhub_tmp）与误建的 D:\c\Users\hdx\.workbuddy 已清理。

## 修复：文案生成与合规审查的“循环 bug”
问题：用户发现文案生成器直接产出“超亮 / 亮度提升300% / 护眼 / 保护视力健康”等表述，再拿去合规审查被打出极限词、医疗宣称类风险。用户质疑这是“循环 bug”。

根因：文案生成提示词未内置合规规则，生成与审查两张皮。

修改：
- `services/prompt_manager.py`：
  - 新增模块级 `COPYWRITER_COMPLIANCE_RULES`（极限词、数据百分比、医疗健康宣称、场景人群、竞品/资质/虚假）；
  - `get_copywriter_prompt` 要求中直接注入该规则；
  - `get_refine_prompt` 增加“去除极限词/医疗宣称/无证据百分比”要求；
  - `get_translate_prompt` 要求翻译时不得引入违规表述，原文含违规则改写；
  - 新增 `get_rewrite_prompt` 用于二次改写。
- `services/llm_service.py`：
  - `generate_copywriter` 增加 `auto_compliance=True` 参数；
  - 生成后自动调用 `scan_compliance` 自检；若命中风险，自动用 `get_rewrite_prompt` 让 LLM 再改一次，返回最终合规版本；
  - 响应新增 `compliance` 字段（`original_clean` / `rewritten` / `notes` / `original_overall_risk`）。
- `frontend/src/components/Copywriter.vue`：结果展示顶部新增合规状态徽标（绿色“已通过生成端合规自检”或黄色“已自动合规改写 n 处”），并展示自动改写记录。

验证：
- 后端 `py_compile` 全通过；
- 前端 `npm run build` 成功（3095 模块）。

用户操作：必须重启 Flask 后端以加载新代码。

## 改写：cross-border-review-skill 改为 WorkBuddy 原生可用版
用户确认需要可用版本。原 SkillHub 装的是 OpenClaw 专用脚本（import openclaw 缺失、install.sh 会 pip 装包、API 写死 demo 数据），在 WorkBuddy 下不可用。
- 已将 `C:\Users\hdx\.workbuddy\skills\cross-border-review-skill\` 重写为 **纯 SKILL.md 指令型技能**（删除 install.sh / cross_border_review_skill.py / usage.txt / lock）。
- 内容：适用场景、4 条核心原则（同语言回复/绝不编造/防封店优先/简洁真诚）、7 步工作流、按情感分类的回复策略、违禁词与风控规则（站外引流/索评返现/虚假承诺/攻击歧视/隐私）、可直接复用的 system+user 提示词模板、去重与 JSON 日志导出格式、可选对接现有 `/api/customer-service` 与 `/api/compliance`、示例。
- 设计对齐项目既有客服模块约定（同语言回复 + response_zh 中文翻译 + 不编造），自包含可直接用 Agent 能力生成，无需 Flask 运行。

## 新增：三个模块的使用记录本地持久化（localStorage）
用户反馈：客服每次刷新记录就没了；文案生成/合规审查也希望有使用记录。诊断：整个前端无任何持久化，状态全在 Vue 内存变量（ref/reactive），刷新即丢；客服的 messages 甚至是写死假数据。
方案（用户选）：浏览器 localStorage，三个模块都加。无需后端 API。

改动：
- 新建 `frontend/src/utils/storage.js`：封装 `loadState/saveState/clearState`（JSON + 前缀 `cb_agent_`，带异常保护）。
- `Copywriter.vue`：新增 `copywriter_history`。页面顶部加"使用记录"栏（手动/链接两种模式统一，含时间戳+摘要+删除/清空），生成成功后 pushHistory，onMounted 恢复，点击历史项 restoreHistory 自动切回对应模式。
- `Compliance.vue`：新增 `compliance_history`。顶部加"使用记录"栏，审查成功后 pushHistory，点击恢复输入+结果，显示风险等级徽标。
- `CustomerService.vue`：原 messages 写死 5 条假数据 → 改为从 `customer_service_messages` 加载；首次用示例数据并落盘，之后以本地真实记录为准；autoReply 成功后 persistMessages；"刷新"按钮绑定 loadMessages（重新读本地）。

验证：前端 `npm run build` 成功（3096 模块）。

用户操作：本次仅改前端，Flask 后端无需重启；前端 dev server 会热更新（或重新 `npm run dev`），刷新页面即可看到各模块新增的“使用记录”栏。

## 升级：方案 B — 后端数据库 + API 持久化（真实可用）
用户确认要真正实现项目（1688 商家可用），选 B：后端 SQLite + REST API。
改动：
- 新建 `backend/utils/db.py`：标准库 sqlite3 封装（get_conn/db_cursor/init_db + 三表 CRUD helper），参数化查询防注入，预留 merchant_id（默认 'default'，为将来多商家/登录留口）。表：copywriter_history / compliance_history / cs_messages。
- `backend/app.py`：启动时调用 init_db()（DB 文件 backend/data/app.db）。
- `controllers/copywriter.py`：新增 `/history` 的 GET/POST/DELETE `/history/<id>`/DELETE `/history`(清空)。
- `controllers/compliance.py`：同上 `/history` 路由；GET 时解析 result_json→result、options_json→options。
- `controllers/customer_service.py`：新增 `/messages` 的 GET/POST/PUT `/messages/<id>`/DELETE `/messages/<id>`。
- 前端 `client.js`：新增 copywriterHistoryAPI / complianceHistoryAPI / customerServiceMessagesAPI；删除 localStorage 版 storage.js。
- 三组件（Copywriter/Compliance/CustomerService）改为 async 调用后端 API；客服去掉写死假数据，新增“新建消息”录入表单（客户名/平台/内容），AI 回复后 PUT 回写。

验证（沙箱真实启动 Flask + curl/Python 端到端）：
- 依赖装到托管 python（flask/flask-cors/marshmallow/requests）；
- /api/health OK；copywriter history POST→GET 中文往返成功（SQLite 持久化生效）；compliance history POST→GET OK；cs messages POST→GET→PUT 全 OK。
- 前端 `npm run build` 成功（3095 模块）。
- 测试残留数据已清理（删除 backend/data/app.db，首次启动自动重建空表）。

用户操作：本地 `pip install -r requirements.txt` && `python app.py`，前端 `npm run dev`；刷新页面即可用真实持久化记录。

## 新增：链接一键生成"最终可上架文本"模块 + 提速
用户要求补全链路最后一步"输出最终可上架文本"，并希望爬虫/生成更快。

改动：
- `services/llm_service.py`：`generate_copywriter` 现把完整风控结果存入 `result['compliance_scan']`；若触发改写，改写后再扫一次最终文本并覆盖 `compliance_scan`（保证风控报告与成品一致）。
- `controllers/auto_listing.py`：
  - 新增 `_build_final_listing(listing, compliance_scan)`：纯文本拼装（Title / Bullet Points / Description）成可直接复制发布的亚马逊字段位文本，并以 `compliance_scan` 判定 `ready`（是否可直接上架）+ `overall_risk`，**不额外调用 LLM**。
  - 步骤4 改为直接复用 `listing['compliance_scan']`，仅在缺失时兜底重扫一次 → **省掉原先重复的 4 次 scan_compliance LLM 调用**。
  - 步骤3 并发数 `max_workers` 3 → 4（四语种真正并行）；步骤5 拼装 final_listings，步骤6 输出并入 `final_listings`。
- `services/scraper.py`：请求模式随机延时 1.5~3.5s → 0.4~1.2s；Playwright 模式懒加载等待 2~4s → 0.6~1.2s（提速，仍保留反爬头与兜底）。
- `frontend/src/components/Copywriter.vue`：新增"板块5 最终可上架文本"（语种切换 + 绿/黄可上架徽标 + 黑色代码块展示 + 复制本语种/复制全部；并接入 copyAllLink 一并复制）；pipelineSteps 更新为 6 步；输入提示补"→ 输出可上架文本"。

验证：后端 py_compile 全过；前端 `npm run build` 成功（3095 模块）。
提速要点：去重合规扫描是主要收益（少 4 次 LLM 往返），并发与爬虫延时压缩为次要收益。
用户操作：须重启 Flask 后端加载新代码（`python app.py`）。
